# Copy to .env and fill in. .env is gitignored — never commit it.
# On GitHub Actions these come from repository secrets instead (see README).

# Jev — TypeSafe AI
JEV_API_KEY=

# OpenRouter — used for Claude, GPT and other classic LLM engines
OPENROUTER_API_KEY=
